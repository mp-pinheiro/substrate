{
  "name": "dbt",
  "claims": {},
  "toolchain": [
    { "bin": "dbt", "hint": "pipx install dbt-core && pipx inject dbt-core dbt-duckdb" }
  ],
  "ci": [
    "sudo apt-get install -y -qq pipx",
    "sudo PIPX_HOME=/opt/pipx PIPX_BIN_DIR=/usr/local/bin pipx install dbt-core==1.12.0",
    "sudo PIPX_HOME=/opt/pipx pipx inject dbt-core dbt-duckdb==1.10.1"
  ],
  "templates": [],
  "checks": ["65-dbt-parse.sh", "66-dbt-manifest.sh"],
  "slop_fixtures": [],
  "check_fixtures": [
    { "file": "fixtures/bad-manifest", "dest": "target", "fails": "66-dbt-manifest.sh" },
    { "file": "fixtures/bad-dbt", "dest": ".", "fails": "65-dbt-parse.sh" }
  ]
}
