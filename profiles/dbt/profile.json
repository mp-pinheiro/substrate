{
  "name": "dbt",
  "claims": {},
  "toolchain": [
    { "bin": "dbt", "hint": "pipx install dbt-core && pipx inject dbt-core dbt-duckdb" }
  ],
  "ci": [
    "sudo apt-get install -y -qq pipx",
    "sudo PIPX_HOME=/opt/pipx PIPX_BIN_DIR=/usr/local/bin pipx install dbt-core",
    "sudo PIPX_HOME=/opt/pipx pipx inject dbt-core dbt-duckdb"
  ],
  "templates": [],
  "checks": ["65-dbt-parse.sh"],
  "slop_fixtures": [],
  "check_fixtures": [{ "file": "fixtures/bad-dbt", "dest": ".", "fails": "65-dbt-parse.sh" }]
}
